plugins {
    id 'com.android.library'
    id 'com.vanniktech.maven.publish' version '0.36.0'
    id 'signing'
}

group = 'com.b3dgs.lionengine'
version = '10.0.0'

android {
    namespace 'com.b3dgs.lionengine.android'
    compileSdk 37

    defaultConfig {
        minSdk 21
        targetSdk 37
        multiDexEnabled true
    }

    compileOptions {
        coreLibraryDesugaringEnabled true
        sourceCompatibility JavaVersion.VERSION_21
        targetCompatibility JavaVersion.VERSION_21
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.app
        }
    }

    signingConfigs {
        app {
            storeFile file(property('keystore'))
            keyAlias property('keystore.alias')
            keyPassword property('keystore.password')
            storePassword property('keystore.storepassword')
        }
    }
}

mavenPublishing {
    publishToMavenCentral()
    signAllPublications()

    configure(new com.vanniktech.maven.publish.AndroidSingleVariantLibrary("release"))

    coordinates(
        "com.b3dgs.lionengine",
        "lionengine-core-android",
        version.toString()
    )

    pom {
        name.set("LionEngine Core Android")
        description.set("LionEngine Core Android implementation")
        url.set("https://github.com/b3dgs/lionengine")

        licenses {
            license {
                name.set("GNU General Public License (GPL) version 3.0")
                url.set("https://www.gnu.org/licenses/gpl-3.0.txt")
            }
        }

        developers {
            developer {
                id.set("DjThunder")
                name.set("Pierre-Alexandre")
                email.set("contact@b3dgs.com")
            }
        }

        scm {
            url.set("https://github.com/b3dgs/lionengine-core-android")
        }
    }
}

dependencies {
    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.1.5'

    implementation 'com.b3dgs:b3dgs-parent:2.0.0'
    implementation 'com.b3dgs.lionengine:lionengine-core:10.0.0'
}