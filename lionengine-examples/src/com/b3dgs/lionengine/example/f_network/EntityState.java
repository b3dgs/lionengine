package com.b3dgs.lionengine.example.f_network;

/**
 * List of entity states.
 */
public enum EntityState
{
    /** Idle state. */
    IDLE,
    /** Walk state. */
    WALK,
    /** turn state. */
    TURN,
    /** Jump state. */
    JUMP,
    /** Dead state. */
    DEAD;
}
