package com.b3dgs.lionengine.example.e_shmup.c_tyrian.entity;

import java.util.Locale;

/**
 * List of entity static types.
 */
public enum EntityStaticType
{
    /** Type A. */
    A,
    /** Type B. */
    B,
    /** Type C. */
    C,
    /** Type D. */
    D,
    /** Type E. */
    E,
    /** Type F. */
    F,
    /** Type G. */
    G,
    /** Type H. */
    H,
    /** Type I. */
    I,
    /** Type J. */
    J,
    /** Type K. */
    K,
    /** Type L. */
    L,
    /** Type M. */
    M,
    /** Type N. */
    N,
    /** Type O. */
    O,
    /** Type P. */
    P,
    /** Type Q. */
    Q,
    /** Type R. */
    R,
    /** Type S. */
    S,
    /** Type T. */
    T;

    @Override
    public String toString()
    {
        return name().toLowerCase(Locale.ENGLISH);
    }
}
